export const metadata = {
    title: "Flavour.Voyage - Sign Up",
    description: "Sign Up to unlock the full potential of Flavour.Voyage",
  };
  
  export default async function RecipesLayout({ children }) {
    return (
        <main>{children}</main>
    );
  }