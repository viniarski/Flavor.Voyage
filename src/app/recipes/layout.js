export const metadata = {
    title: "Flavour.Voyage - Recipes",
    description: "Browse through recipes available",
  };
  
  export default async function RecipesLayout({ children }) {
    return (
        <main>{children}</main>
    );
  }