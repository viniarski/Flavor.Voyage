export const metadata = {
    title: "Flavour.Voyage - Contact Us",
    description: "Give us feedback or report an issue.",
  };
  
  export default async function RecipesLayout({ children }) {
    return (
        <main>{children}</main>
    );
  }