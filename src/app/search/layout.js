export const metadata = {
    title: "Flavour.Voyage - Search Results",
    description: "Results from your search",
  };
  
  export default async function RecipesLayout({ children }) {
    return (
        <main>{children}</main>
    );
  }