export const metadata = {
    title: "Flavour.Voyage - New Blog Post",
    description: "Add a new Blog Post.",
  };
  
  export default async function RecipesLayout({ children }) {
    return (
        <main>{children}</main>
    );
  }