export const metadata = {
    title: "Flavour.Voyage - Blog Posts",
    description: "Check out our latest blogs.",
  };
  
  export default async function RecipesLayout({ children }) {
    return (
        <main>{children}</main>
    );
  }