export const metadata = {
    title: "Flavour.Voyage - About Us",
    description: "We are a group of passionate people who are passionate about cooking and sharing our knowledge with the world.",
  };
  
  export default async function RecipesLayout({ children }) {
    return (
        <main>{children}</main>
    );
  }