export const metadata = {
    title: "Flavour.Voyage - Sign In",
    description: "Sign In to unlock the full potential of Flavour.Voyage",
  };
  
  export default async function RecipesLayout({ children }) {
    return (
        <main>{children}</main>
    );
  }